{
  "name": "killswitch-wallet",
  "private": true,
  "scripts": {
    "vercel-build": "bash apps/console/assemble-server.sh && npm install --prefix agent && npm run build --prefix agent && npm install --prefix apps/console",
    "console": "node apps/console/server.mjs",
    "assemble-console": "bash apps/console/assemble-server.sh"
  }
}
