
    /* --- Auto-demo guided tour / spotlight --- */
    #demoTourBanner {
      display: none;
      position: sticky;
      top: 0;
      z-index: 9000;
      margin: 0;
      padding: 10px 16px;
      background: linear-gradient(90deg, rgba(0,20,40,0.97), rgba(40,0,50,0.95));
      border-bottom: 1px solid rgba(0,246,255,0.45);
      box-shadow: 0 8px 32px rgba(0,0,0,0.55), 0 0 28px rgba(0,246,255,0.18);
      backdrop-filter: blur(14px);
      font-size: 13px;
      line-height: 1.4;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }
    #demoTourBanner.show { display: flex; }
    #demoTourBanner .dt-step {
      flex: 0 0 auto;
      font-family: var(--mono);
      font-size: 11px;
      font-weight: 800;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--cyan);
      padding: 4px 10px;
      border-radius: 999px;
      border: 1px solid rgba(0,246,255,0.45);
      background: rgba(0,246,255,0.1);
    }
    #demoTourBanner .dt-title { flex: 1 1 200px; font-weight: 700; color: var(--text); }
    #demoTourBanner .dt-look { flex: 1 1 220px; color: var(--muted); font-size: 12px; }
    #demoTourBanner .dt-look em { color: var(--magenta); font-style: normal; font-weight: 700; }
    #demoSpotlightOverlay {
      display: none;
      position: fixed;
      inset: 0;
      z-index: 8500;
      background: rgba(2,6,14,0.55);
      pointer-events: none;
      transition: opacity 0.25s ease;
    }
    #demoSpotlightOverlay.show { display: block; }
    .demo-spotlight {
      position: relative;
      z-index: 8600 !important;
      outline: 2px solid rgba(0,246,255,0.95) !important;
      outline-offset: 4px;
      box-shadow:
        0 0 0 6px rgba(0,246,255,0.18),
        0 0 40px rgba(0,246,255,0.45),
        0 0 80px rgba(255,43,214,0.25) !important;
      border-radius: 14px;
      animation: demoSpotlightPulse 1.8s ease-in-out infinite;
    }
    @keyframes demoSpotlightPulse {
      0%, 100% { outline-color: rgba(0,246,255,0.9); }
      50% { outline-color: rgba(255,43,214,0.95); }
    }
